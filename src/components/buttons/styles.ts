"use client";

import styled from "styled-components";
import { YELLOW_COLOR } from "@/constants/styles";

export const PrimaryButtonContainer = styled.button`
  padding: 18px 88.5px;
  background-color: ${YELLOW_COLOR};
  border-radius: 4px;
  border: none;
  outline: none;
  cursor: pointer;
  font-size: 16px;
  font-weight: 400;
  font-family: inherit;
`;
